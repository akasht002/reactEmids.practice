import {
    WorkHistory
} from './actions'

const defaultState = {
    
};

const WorkHistoryState = (state = defaultState, action) => {
    switch (action.type) {

        default:
            return state;
    }
}

export default WorkHistoryState;
